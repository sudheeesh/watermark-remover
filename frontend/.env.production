# Backend origin used by the production (Vercel) build.
# Change this if the Render service URL changes. You can also override it
# with a VITE_API_BASE env var in the Vercel dashboard.
VITE_API_BASE=https://watermark-remover-1-2han.onrender.com
